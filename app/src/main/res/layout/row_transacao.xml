<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/img_row_transacao"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="TODO"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/first_line_transacao"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/second_line_transacao"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/img_row_transacao"
        android:gravity="center_vertical"
        android:text="Example application"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/second_line_transacao"
        android:layout_width="wrap_content"
        android:layout_height="26dip"
        android:paddingHorizontal="10dp"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@id/img_row_transacao"
        android:textStyle="bold"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textSize="17sp"
        android:background="@drawable/shape_btn_arredondado_cinza"
        android:text="Description" />

</RelativeLayout>